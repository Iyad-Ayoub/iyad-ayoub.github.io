import { SectionHeading } from '../components/SectionHeading'
import { publications } from '../data/publications'
const profiles = [{ label: 'Google Scholar', url: 'https://scholar.google.com/citations?user=Soz9gVgAAAAJ&hl=ar' }, { label: 'ORCID', url: 'https://orcid.org/0000-0002-5116-2662' }, { label: 'ResearchGate', url: 'https://www.researchgate.net/profile/Im-Abuhadrous' }]

export function PublicationsSection() { return <section className="section" id="publications"><div className="container"><SectionHeading eyebrow="Selected publications" title="Research record." />{publications.length > 0 ? <div className="publication-list">{publications.map((publication, index) => <article className="publication" key={`${publication.title}-${index}`}><span className="publication-number">0{index + 1}</span><div><h3>{publication.title}</h3><p>{publication.authors}</p><p className="publication-meta">{[publication.venue, publication.year].filter(Boolean).join(' · ')}</p></div></article>)}</div> : <div className="publication-empty"><p>Selected publications in autonomous systems, robotics, perception, and intelligent vehicles will be listed here. Complete publication profiles are available through Google Scholar and ORCID.</p><div className="card-links">{profiles.map((profile) => <a href={profile.url} key={profile.label} target="_blank" rel="noopener noreferrer">{profile.label} <span aria-hidden="true">↗</span></a>)}</div></div>}</div></section> }
