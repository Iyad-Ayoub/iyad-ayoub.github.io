import type { ResearchProject } from '../types'
const links = [{ key: 'repositoryUrl', label: 'Repository' }, { key: 'publicationUrl', label: 'Publication' }, { key: 'projectUrl', label: 'Project' }] as const
export function ProjectCard({ project }: { project: ResearchProject }) { return <article className="project-card">{(project.status || project.image) && <div className="project-top">{project.status && <span className="status">{project.status}</span>}{project.image && <img src={project.image} alt="" />}</div>}<h3>{project.title}</h3><p>{project.description}</p><ul className="tag-list">{project.technologies.map((technology) => <li key={technology}>{technology}</li>)}</ul>{links.some(({ key }) => project[key]) && <div className="card-links">{links.map(({ key, label }) => project[key] && <a key={key} href={project[key]} target="_blank" rel="noopener noreferrer">{label}</a>)}</div>}</article> }
